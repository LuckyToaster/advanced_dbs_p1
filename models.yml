Person:
  required_vars:
    - name
    - surname
  admissible_vars:
    - dni
    - email
    - age
    - address
  unique_indexes:
    - dni
    - email
  regular_indexes:
    - name
    - surname
    - age
  location_index: address

Company:
  required_vars:
    - name
  admissible_vars:
    - dni
    - address
  unique_indexes:
    - dni
  regular_indexes:
    - name
  location_index: address

EducationalCentre:
  required_vars:
    - name
  admissible_vars:
    - address
  unique_indexes:
    - name
  regular_indexes:
    - address
  location_index: address

